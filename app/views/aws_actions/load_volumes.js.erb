$('span#load-all-vols').html('<%= escape_javascript select_tag("scheduled_snapshot_volume_id", options_for_select(@volumes.collect{ |u| [u.volume_id, u.volume_id] }), name: "scheduled_snapshot[volume_id][]", class: "span3") %>');
